version: "3.8"
services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: host
      POSTGRES_PASSWORD: host
      POSTGRES_DB: test
